CREATE TABLE IF NOT EXISTS CoinCapApiIds (
    ticker TEXT PRIMARY KEY,
    id TEXT UNIQUE NOT NULL
);

insert:
INSERT OR REPLACE INTO CoinCapApiIds(ticker, id)
VALUES (?, ?);

selectByTicker:
SELECT id
FROM CoinCapApiIds
WHERE ticker = ?;
